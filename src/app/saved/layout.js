export const metadata = {
  title: "Saved Items",
  description: "Your bookmarked and saved content on TechForDev.",
  robots: { index: false, follow: false },
};

export default function SavedLayout({ children }) {
  return children;
}

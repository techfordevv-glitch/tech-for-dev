export const metadata = {
  title: "Settings",
  description: "Manage your TechForDev account settings, preferences, and notifications.",
  robots: { index: false, follow: false },
};

export default function SettingsLayout({ children }) {
  return children;
}

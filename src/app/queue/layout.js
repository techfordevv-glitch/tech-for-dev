export const metadata = {
  title: "Reading Queue",
  description: "Your personal reading queue on TechForDev.",
  robots: { index: false, follow: false },
};

export default function QueueLayout({ children }) {
  return children;
}
